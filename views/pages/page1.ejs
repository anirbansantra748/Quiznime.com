<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
    crossorigin="anonymous"
  />
  <link rel="stylesheet" href="stylle.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-position: center;
      margin: 0;
      padding: 0;
      background-image: url("./public/zenu_clipdrop-enhance.jpg") !important;
      background-color: #665c5c;
    }

    form {
      margin-top: 5rem;
      display: flex;
      justify-content: center;
    }

    .card {
      width: 28rem; /* Adjust the width as needed */
      height: auto; /* Adjust the height as needed */
      background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent white background */
      border: 1px solid #ddd;
      border-radius: 15px;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
      position: relative;
      overflow: hidden;
      margin-top: -2.5rem;
    }

    .QuestionImg {
      max-width: 100%;
      height: 15rem;
    }

    .card-body {
      text-align: center;
    }

    .card-title {
      font-size: 24px;
      font-weight: bold; /* Make the question title bold */
      margin-bottom: 1rem;
    }

    .card-text {
      font-weight: bold; /* Make the score text bold */
      margin: 1rem 0;
    }

    .score {
      text-align: right;
      padding: 0 1rem;
      font-size: 18px;
    }

    .list-group-item {
      list-style: none; /* Remove bullet points for the radio buttons */
      padding: 0.5rem 0;
    }

    label {
      display: flex;
      align-items: center;
      
    }

    input[type="radio"] {
      margin-right: 0.5rem;
      
    }

    .btn {
      background-color: #FFA500; /* Orange background color for the submit button */
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 10px;
      cursor: pointer;
      font-size: 18px;
      display: flex;
      justify-content: center;
      align-items: center;
      transition: background-color 0.3s;
      margin: 1rem auto;
      display: block;
    }

    .btn:hover {
      background-color: #FF7F00; /* Darker orange on hover */
    }

    .back-button {
      position: absolute;
      top: 10px;
      left: 10px;
      background-color: #FFA500; /* Orange background color for the back button */
      color: white;
      border: none;
      border-radius: 50%;
      cursor: pointer;
      font-size: 24px;
      display: flex;
      justify-content: center;
      align-items: center;
      width: 40px;
      height: 40px;
      transition: background-color 0.3s;
    }

    .back-button:hover {
      background-color: #FF7F00; /* Darker orange on hover */
    }

    .round-button {
      position: absolute;
      top: 10px;
      right: 10px;
      background-color: #FFA500; /* Orange background color for the round button */
      color: white;
      border: none;
      border-radius: 50%;
      cursor: pointer;
      font-size: 24px;
      display: flex;
      justify-content: center;
      align-items: center;
      width: 40px;
      height: 40px;
      transition: background-color 0.3s;
    }

    .round-button:hover {
      background-color: #FF7F00; /* Darker orange on hover */
    }
  </style>
</head>

<body>
  <div class="container">
    <a href="/quizanime/home" class="back-button">&#9664;</a> <!-- Use the appropriate icon for your back button -->
  <div class="round-button">🏠</div> <!-- Replace '🏠' with the icon of your choice -->
  <form method="POST" action="/quizanime/page1">
    <div class="row">
      <div class="col">
        <div class="card p-1">
          <img src="<%= question.url %>" class="card-img-top QuestionImg" alt="...">
          <div class="card-body">
            <h5>Question no <%= length + 1 %>/<%= totalQuestions %></h5>
            <h5 class="card-title"><%= question.question %></h5>
            <div class="score">Your Score: <%= score %></div>
          </div>
          <ul class="list-group list-group-flush">
            <% for(option of question.options){ %>
              <li class="list-group-item">
                <label>
                  &nbsp;
                  <input type="radio" name="userAnswer" value="<%= option %>" required>
                  &nbsp;<%= option %>
                </label>
              </li>
            <% } %>
          </ul>
          <button class="btn" type="submit">Submit</button>
        </div>
      </div>
    </div>
  </form>
  </div>
</body>

</html>





